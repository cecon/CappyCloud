O agente foi movido para `services/cappycloud_agent/` e é invocado pela API FastAPI (`services/api/`).

A imagem base **Open WebUI Pipelines** (`services/pipelines/Dockerfile` antigo) deixou de ser usada pelo `docker-compose` atual.
