diff --git a/src/grpc/server.ts b/src/grpc/server.ts
index 69bfdf4..7f9d9e4 100644
--- a/src/grpc/server.ts
+++ b/src/grpc/server.ts
@@ -7,6 +7,7 @@ import { getTools } from '../tools.js'
 import { getDefaultAppState } from '../state/AppStateStore.js'
 import { AppState } from '../state/AppState.js'
 import { FileStateCache, READ_FILE_STATE_CACHE_SIZE } from '../utils/fileStateCache.js'
+import { getMcpToolsCommandsAndResources } from '../services/mcp/client.js'
 
 const PROTO_PATH = path.resolve(import.meta.dirname, '../proto/openclaude.proto')
 
@@ -85,11 +86,30 @@ export class GrpcServer {
 
           const toolNameById = new Map<string, string>()
 
+          // Load MCP servers from ~/.claude/settings.json (written by CappyCloud pipeline)
+          const grpcMcpClients: any[] = []
+          const grpcMcpTools: any[] = []
+          const grpcMcpCommands: any[] = []
+          try {
+            await getMcpToolsCommandsAndResources(({ client, tools, commands }) => {
+              grpcMcpClients.push(client)
+              grpcMcpTools.push(...tools)
+              grpcMcpCommands.push(...commands)
+            })
+          } catch (mcpErr: any) {
+            console.warn("[grpc] MCP load warning:", mcpErr?.message ?? mcpErr)
+          }
+          const grpcBaseTools = getTools(appState.toolPermissionContext)
+          const grpcToolsByName = new Map<string, any>()
+          for (const tool of [...grpcBaseTools, ...grpcMcpTools]) {
+            grpcToolsByName.set(tool.name, tool)
+          }
+
           engine = new QueryEngine({
             cwd: req.working_directory || process.cwd(),
-            tools: getTools(appState.toolPermissionContext), // Gets all available tools
-            commands: [], // Slash commands
+            tools: Array.from(grpcToolsByName.values()),
+            commands: grpcMcpCommands,
-            mcpClients: [],
+            mcpClients: grpcMcpClients,
             agents: [],
             ...(previousMessages.length > 0 ? { initialMessages: previousMessages } : {}),
             includePartialMessages: true,
