@AGENTS.md

## Só para o Claude Code

- As skills de design em `.claude/skills/` (`design-system`, `ui-styling`,
  `ui-ux-pro-max`) são carregadas automaticamente.
- As skills de domínio ficam em `.agents/skills/<nome>/SKILL.md` (o Codex as
  carrega sozinho). Quando o assunto bater, leia o `SKILL.md` com a ferramenta
  Read antes de começar. Lista em [docs/SKILLS.md](docs/SKILLS.md).
- Não faz deploy nem altera infraestrutura sem pedido explícito.
